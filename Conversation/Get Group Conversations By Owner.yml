info:
  name: Get Group Conversations By Owner
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/conversation/my-groups"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
