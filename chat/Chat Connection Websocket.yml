info:
  name: Chat Connection Websocket
  type: websocket
  seq: 1

websocket:
  url: "{{url_ws}}/chat/ws/5"
  message:
    - title: message 1
      selected: false
      message:
        type: json
        data: |-
          {
            "conversation_id":6,
            "message":"noobs go to zoo suckers , HAHAHAHHA"
          }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  timeout: 0
  keepAliveInterval: 0
