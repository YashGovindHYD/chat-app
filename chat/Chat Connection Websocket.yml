info:
  name: Chat Connection Websocket
  type: websocket
  seq: 1

websocket:
  url: "{{url_ws}}/chat/ws/1"
  message:
    - title: message 1
      selected: false
      message:
        type: json
        data: |-
          {
            "conversation_id":1,
            "message":"HEY HOW ARE YOU ALL PEEPS SMILY :)))))"
          }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  timeout: 0
  keepAliveInterval: 0
