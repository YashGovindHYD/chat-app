info:
  name: Logout
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/auth/logout"
  auth: inherit

runtime:
  variables:
    - name: url
      value: http://127.0.0.1:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
