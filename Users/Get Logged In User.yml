info:
  name: Get Logged In User
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/users/me"
  auth: inherit

runtime:
  variables:
    - name: url
      value: http://127.0.0.1:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
