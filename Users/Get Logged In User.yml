info:
  name: Get Logged In User
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
